namespace IOU1.Tests;

public class UserTests
{
    //var employeeContextMock = new Mock<EmployeeDBContext>();
    //employeeContextMock.Setup<DbSet<Employee>>(x => x.Employees)
    //.ReturnsDbSet(TestDataHelper.GetFakeEmployeeList());

    //await _serviceBus.PublishAsync(new TestMessage() { Mess = "wiadomość do wysłania" }, cancellationToken);
    //    await _serviceBus.SendAsync(new TestMessage() { Mess = "wiadomość do wysłania" }, "TestQueue");

}
